<aside class="floating-inquiry" aria-label="在线咨询表单">
  <details>
    <summary aria-label="打开在线咨询表单">
      <i class="fa-solid fa-message" aria-hidden="true"></i>
      <span>在线咨询</span>
    </summary>

    <form class="inquiry-panel glass-panel" action="https://api.web3forms.com/submit" method="POST">
      <input type="hidden" name="access_key" value="a28877b3-18aa-41a4-9b8d-b96185712e80" />
      <input type="hidden" name="subject" value="SEO小平网站咨询表单" />
      <input type="checkbox" name="botcheck" class="botcheck" tabindex="-1" autocomplete="off" />

      <div class="panel-heading">
        <p>快速咨询</p>
        <span>留下需求，我会尽快回复。</span>
      </div>

      <label>
        <span>姓名</span>
        <input type="text" name="name" required autocomplete="name" placeholder="请输入您的姓名" />
      </label>

      <label>
        <span>邮箱</span>
        <input type="email" name="email" required autocomplete="email" placeholder="请输入您的邮箱" />
      </label>

      <label>
        <span>需求</span>
        <textarea name="message" required rows="4" placeholder="请简单描述您的网站或SEO需求"></textarea>
      </label>

      <button type="submit">
        <i class="fa-solid fa-paper-plane" aria-hidden="true"></i>
        <span>提交咨询</span>
      </button>
    </form>
  </details>
</aside>

<style>
  .floating-inquiry {
    position: fixed;
    right: clamp(0.75rem, 2vw, 1.25rem);
    top: 50%;
    z-index: 70;
    transform: translateY(-50%);
  }

  details {
    position: relative;
  }

  summary {
    display: grid;
    min-width: 3.1rem;
    min-height: 8.5rem;
    place-items: center;
    gap: 0.5rem;
    border: 1px solid rgba(255, 255, 255, 0.78);
    border-radius: 8px;
    background: rgba(239, 125, 0, 0.92);
    color: #fff;
    cursor: pointer;
    list-style: none;
    padding: 0.85rem 0.6rem;
    box-shadow: 0 18px 48px rgba(239, 125, 0, 0.28);
    transition:
      transform 180ms ease,
      box-shadow 180ms ease,
      background 180ms ease;
  }

  summary::-webkit-details-marker {
    display: none;
  }

  summary:hover,
  details[open] summary {
    background: #d96f00;
    box-shadow: 0 22px 54px rgba(239, 125, 0, 0.34);
    transform: translateX(-0.15rem);
  }

  summary span {
    writing-mode: vertical-rl;
    font-size: 0.92rem;
    font-weight: 850;
    letter-spacing: 0;
  }

  summary i {
    font-size: 1.05rem;
  }

  .inquiry-panel {
    position: absolute;
    right: calc(100% + 0.75rem);
    top: 50%;
    display: grid;
    width: min(20rem, calc(100vw - 6rem));
    gap: 0.85rem;
    padding: 1rem;
    border-radius: 8px;
    transform: translateY(-50%);
  }

  .panel-heading {
    display: grid;
    gap: 0.25rem;
  }

  .panel-heading p {
    margin: 0;
    color: var(--color-text);
    font-size: 1.05rem;
    font-weight: 900;
    line-height: 1.25;
  }

  .panel-heading span {
    color: rgba(31, 21, 15, 0.62);
    font-size: 0.86rem;
    line-height: 1.6;
  }

  label {
    display: grid;
    gap: 0.35rem;
  }

  label span {
    color: rgba(31, 21, 15, 0.72);
    font-size: 0.86rem;
    font-weight: 800;
  }

  input,
  textarea {
    width: 100%;
    border: 1px solid rgba(239, 125, 0, 0.16);
    border-radius: 8px;
    background: rgba(255, 255, 255, 0.78);
    color: var(--color-text);
    font: inherit;
    outline: none;
    padding: 0.72rem 0.78rem;
    transition:
      border-color 180ms ease,
      box-shadow 180ms ease,
      background 180ms ease;
  }

  textarea {
    min-height: 7rem;
    resize: vertical;
  }

  input:focus,
  textarea:focus {
    border-color: rgba(239, 125, 0, 0.58);
    background: #fff;
    box-shadow: 0 0 0 3px rgba(239, 125, 0, 0.12);
  }

  button {
    display: inline-flex;
    min-height: 2.8rem;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    border: 0;
    border-radius: 8px;
    background: var(--color-primary);
    color: #fff;
    cursor: pointer;
    font: inherit;
    font-weight: 850;
    box-shadow: 0 14px 34px rgba(239, 125, 0, 0.24);
    transition:
      filter 180ms ease,
      transform 180ms ease;
  }

  button:hover {
    filter: brightness(1.04);
    transform: translateY(-1px);
  }

  .botcheck {
    position: absolute;
    left: -9999px;
    opacity: 0;
  }

  @media (max-width: 760px) {
    .floating-inquiry {
      top: auto;
      right: 0.85rem;
      bottom: 0.85rem;
      transform: none;
    }

    summary {
      display: inline-flex;
      min-width: 0;
      min-height: 3rem;
      gap: 0.45rem;
      padding: 0 1rem;
    }

    summary span {
      writing-mode: horizontal-tb;
    }

    summary:hover,
    details[open] summary {
      transform: translateY(-1px);
    }

    .inquiry-panel {
      position: fixed;
      right: 0.85rem;
      bottom: 4.35rem;
      top: auto;
      width: min(21rem, calc(100vw - 1.7rem));
      transform: none;
    }
  }
</style>
